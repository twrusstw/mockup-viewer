name: Beta Release

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+-beta.[0-9]+'

jobs:
  build_and_beta_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify tag is from dev branch
        run: |
          if ! git branch -r --contains ${{ github.sha }} | grep -q "origin/dev"; then
            echo "❌ Beta tags must originate from the dev branch"
            exit 1
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build plugin
        run: npm run build

      - name: Validate version consistency
        run: |
          TAG_VERSION=${{ github.ref_name }}
          BASE_VERSION=$(echo "$TAG_VERSION" | sed 's/-beta\.[0-9]*$//')
          MANIFEST_VERSION=$(jq -r '.version' manifest.json)
          if [ "$BASE_VERSION" != "$MANIFEST_VERSION" ]; then
            echo "❌ Version mismatch: tag base=$BASE_VERSION manifest=$MANIFEST_VERSION"
            exit 1
          fi

      - name: Extract CHANGELOG section
        run: |
          TAG=${{ github.ref_name }}
          BASE=$(echo "$TAG" | sed 's/-beta\.[0-9]*$//')
          awk "/^## \\[$BASE\\]/{flag=1; next} /^## \\[/{flag=0} flag" CHANGELOG.md > /tmp/notes.md
          if [ ! -s /tmp/notes.md ]; then
            echo "❌ CHANGELOG.md has no entry for $BASE"
            exit 1
          fi

      - name: Create or Update GitHub Pre-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG=${{ github.ref_name }}

          if gh release view "$TAG" > /dev/null 2>&1; then
            echo "Release $TAG already exists. Updating assets..."
          else
            echo "Creating new pre-release $TAG..."
            gh release create "$TAG" \
              --title "$TAG (Beta)" \
              --notes-file /tmp/notes.md \
              --prerelease
          fi

          gh release upload "$TAG" \
            main.js \
            manifest.json \
            styles.css \
            --clobber
